Okay, here I'm going to talk about the comprehensive overall flow of what I'm going to achieve in terms of the theory of population and evolution strategies in LLM fine-tuning. The core argument is that:
1. The backdrop is that we want to investigate the effect of population in ES fine-tuning, and our bottom line is that, when you're using cross-entropy setting, it doesn't matter; just use one or whatever.
2. More importantly, the non-trivial result is that, with binary reward (which is non-cross-entropy, not continuous and non-differentiable), I'll just call it binary, but it's interchangeably accuracy or F1 score. In this setting, discrete, non-differentiable, there is a minimum n to beat the low signal-to-noise ratio of this accuracy reward, and therefore we should set population above that minimal n.Also, above that, a minimal n would be a right hyperparameter setup, i.e., scaling the learning rate accordingly to the population and within a fixed budget. It doesn't really matter, but of course there could be some waste in a computational budget, so it should still be the upper bound, the practical upper bound. It will be the effective rank of the Hessian, and above that there's waste in your population.


So that was my argument, and another backdrop from previous literatures: we know that in the past people have argued that the zero-chloride method might not work in LLM because of the curse of dimensionality. Since the variance of the gradient estimator of ES scales up proportionally to the dimension, we need D times more steps than period-based methods, which in the case of LLM is like too many, right? Empirical results have testified that ES works pretty well in LLM fine-tuning, and that N = 30 seems like an ideal setup, right, but we haven't really known why that works. The recent paper, "The Blessing of Dimensionality," explained that the scaling, the curse of dimensionality, is actually not true because the LLM fine-tuning landscape has a low effective rank, and therefore the gradient of the estimator scales with D, which is the effective rank of the Hessian. What I want to do in my paper is expand this further and understand why N = 30 is ideal and why it doesn't really work under that. That's like maybe N = 30 is like the right number to gather a meaningful signal, right, but the core meaning of this paper is to rigorously and formally investigate that.

So to do that, we first have to introduce something that people have not really paid attention to, which is the reward function, because when you look at previous literatures, approximately half of ES papers in LLM settings have used cross-entropy loss, whereas half of them use accuracy loss, which is very different. Papers that used cross-entropy loss, namely the Meso paper, are interesting because they only use the population of one and still it worked for them, whereas this ES at scale paper or the Blessing of Dimensionality paper used and claimed that an approximately dirty worked well. These seemingly paradoxical results came from the design of the reward function, where in cross-entropy loss whatever n is fine, and therefore n equals one definitely worked, whereas for accuracy rewards or any discrete rewards, it has to be above a certain threshold. That's why I'm arguing that the existence of minimum m depends on the reward type, and in the accuracy reward, that exists. This is very interesting, because if you think about it, the useful case, the case where yes will be very useful as well. There are multiple reasons why people are keen on using yes:
1. Memory efficiency
2. Distributability, which means that every random perturbation is independent, so you can scale up to multiple GPUs
3. The third reason, which I believe is the most important one, is that it can work in a black box reward or black box model  This is really important, because if you can't really know the output and you can only see the final results, how are you going to fine-tune it? That's why I think accuracy reward makes more sense, and cross-entropy, although perfect and not really dependent on, has a limitation in simple and single-token classification test or single-token test like classification, multi-choice, or just question and answer now, and that's exactly what the Meso paper used, so that explains it.So then what's the thing about accuracy reward? The reason why accuracy reward has a minimum and cross-entropy doesn't have one is because accuracy has a low signal-to-noise ratio. 

Therefore, especially if we are fine-tuning with a model that doesn't really have that good of a performance in the task, as the task gets more complicated, the signal-to-noise ratio will be lower. This is because the perturbed model wouldn't be able to generate something meaningful, so it's really about the noise dominating the population and therefore leading the update into a wrong direction. It's not about marginally improving and staying there. It's actually getting worse, because if the noise dominates, which is the case (because the gradient in this direction is relatively small compared to the direction uphill), most directions are uphill, therefore, which is equivalent to saying that of a low signal-to-noise ratio. I believe that was the case for previous papers, setting or empirically finding that any approximately dirty makes sense for specific model and task combination. I really wanted to generalize this and then provide theoretical ground for this, so my work is about how accuracy rewards have got a completely different landscape to cost entropy reward, stemming from its nature of being discrete and non-differentiable. Because of this low signal-to-noise ratio, we try to extract the minimum n to beat that, what I call, a noise wall. My hypothesis was that below this minimum n, the noise dominates the gradient signal, therefore the progress actually gets worsened or the loss actually gets worsened. Above or at the minimum n, statistically we can say that the loss will improve with a certain probability. Our job was to just find that n minimum, so that's one thing that we found out: that n minimum exists and how to calculate that, which is all laid out in the theory markdown work files and also in the relationship to the blessing of dimensionality paper. I found out that above R, which is the effective rank of the Hessian, there is no point in increasing n, and you better just decrease n below R, because above R there is some wasting of wasted computations or perturbations. That's the second thing. The last thing is that between this upper bound and lower bound (upper bound of R and lower bound of n minimum), assuming we have a fixed computational budget or fixed forward pass budget and assuming we have scaled the learning rate accordingly to n, n doesn't really matter. We call it n cancellation. This is also a meaningful result because you don't have to worry about it, so you can just set that equal to dirty once you've verified that it's good, so you don't have to read, no longer have to find a better n, right? This makes sense. If you don't want to bother to scale the learning rate, then assuming both n and the scaled up n prime are between that sandwich bound, you just have to pick the smaller n because you could converge faster with a fixed learning rate that is scaled.

All right, so that's all we found. Oh, and one more thing. We actually wanted to empirically verify this. Not only did we run, and by the way, these empirical results will all come soon. I don't have an explicit figure or any result for that, but I'm still running it. We're not going to even talk about it in this theory draft, but still:
1. We empirically verified that N approximately 30 or any end minimum exists by running different populations across different model and task suite combinations.
2. We also showed that this end minimum actually follows the rule that we said, saying we ran hundreds of perturbations on the base model and then found out that P(A=0), which is based on the batch size of the P0.  Thereby we sort of theoretically explained why N approximately 30 was the minimum ground lower bound for those tasks and model set that previous papers used.

Lastly, we also have to sort of clarify what kind of assumptions we've used for this, plus what kind of previous results that we've developed this based off of.

Alright, so that was the complete flow of what I'm going to write on this theory draft, excluding the empirical results.
